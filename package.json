{
  "name": "netx",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @netx/shell dev",
    "dev:server": "pnpm --filter @netx/server dev",
    "start": "pnpm --filter @netx/server dev & pnpm --filter @netx/shell dev",
    "build": "pnpm -r build",
    "typecheck": "tsc -b",
    "lint": "eslint packages/",
    "test": "vitest"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "better-sqlite3"]
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
